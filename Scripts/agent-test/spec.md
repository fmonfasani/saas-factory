# CRUD de Tasks

Requisitos:

- FastAPI backend
- Endpoints:
  - POST /tasks
  - GET /tasks
  - GET /tasks/{id}
  - PUT /tasks/{id}
  - DELETE /tasks/{id}

Validaciones:
- DELETE debe devolver 204 sin body
- Usar in-memory DB con autoincrement
