name: Deploy to Staging

on:
  push:
    branches: [ develop ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: saasfactory/backend,saasfactory/frontend
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=staging

      - name: Build and push backend image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: Dockerfile.backend
          push: true
          tags: saasfactory/backend:staging,saasfactory/backend:${{ github.sha }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: Dockerfile.frontend
          push: true
          tags: saasfactory/frontend:staging,saasfactory/frontend:${{ github.sha }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Deploy to Staging Environment
        run: |
          echo "Deploying to staging environment..."
          # Add your staging deployment commands here
          # For example, deploying to Railway, Vercel, or another platform
          
      - name: Health Check
        run: |
          echo "Performing health checks..."
          # Add health check commands here
          # curl -f https://staging.yourapp.com/health || exit 1